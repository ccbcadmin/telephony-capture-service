FROM node:latest

# Create app directory
RUN mkdir -p /usr/src/service
WORKDIR /usr/src/service

# Install app dependencies
COPY package.json /usr/src/service
RUN npm install

# Bundle app source
COPY ./lib/ /usr/src/service/lib/

CMD [ "node", "lib/telephony-simulator.js"]

MAINTAINER Rod Monk
