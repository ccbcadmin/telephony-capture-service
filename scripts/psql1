#!/bin/bash

PROGNAME=$(basename $0)

source ./scripts/bash-functions

is-pg1-active && docker exec -it pg1 psql --username postgres -p 5432 -d $TCSENV || true && exit 0;
container-exists pg1 && echo 'pg1 STOPPED' && exit 0;
echo 'pg1 GONE'
exit 0
